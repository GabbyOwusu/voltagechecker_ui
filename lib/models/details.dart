import 'package:flutter/material.dart' show Color;

class CardDetails {
  String title;
  String icon;
  Color color;
  Color textColor;
  Color iconColor;

  CardDetails({
    this.title,
    this.icon,
    this.color,
    this.textColor,
    this.iconColor,
  });
}
